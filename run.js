/**
 * Run.js allows for the app to be executed on demand instead of through a
 * wrapper.
 */

// Imports
// =============================================================================

/**
 * The main app.
 * @type {Object}
 */
var app = require('./sobject-doc-node.js');

// Local execution
// =============================================================================

// Run the app
app.run();
